# Hooks e wrappers polyglot rodam no bash (Git for Windows / Unix) — LF
# obrigatório: CRLF quebra o bash em Linux/macOS (exit 127). cmd.exe aceita LF.
hooks/* text eol=lf
*.cmd text eol=lf
install.sh text eol=lf
tests/* text eol=lf
