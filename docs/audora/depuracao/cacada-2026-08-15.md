# Caçada — repositório audora-commander — 2026-08-15

Executada pela skill `depurar` (modo caçada), primeira rodada real — serve de
teste de aceite da própria skill (nó `skill-depurar`, critério 3).

**Classes varridas:** referências cruzadas, contratos/schemas,
contagens/documentação viva, bordas de erro, configuração/execução.

| # | Classe | Achado | Verificação | Veredito | Ação |
|---|---|---|---|---|---|
| A1 | Contagens/doc viva | PRD.md dizia "7 skills" (3 menções) e arquitetura sem `depurar`, com a skill já na main | `grep "7 skills" PRD.md` → 3 linhas; `grep -c depurar PRD.md` → 0 | confirmado | Corrigido — commit `fix(cacada-A1)` |
| A2 | Contratos/schemas | Índice do GRAFO com 5 nós, só 2 corpos; template não definia se nó `planejada` pode ser index-only | `grep -c "^- .* \| " GRAFO.md` → 5; `grep -c "^### " GRAFO.md` → 2; template lido — regra ausente | confirmado | Regra 0 adicionada ao template (planejada index-only permitido; em-curso+ exige corpo) — commit `fix(cacada-A2)` |
| A3 | Doc viva | Critério 1 do nó plugin-v0.1.0 dizia "7 skills"; README checklist diz 8 — divergência nó vs README | leitura dos dois artefatos, linhas 51 (GRAFO) e checklist (README) | confirmado | Delta MODIFICADO registrado no nó (mecanismo correto do framework) — commit `fix(cacada-A3)` |
| A4 | Doc histórica | Spec de design menciona "7 skills" (§3, §6) | `grep -n "7 skills" docs/specs/...` → 2 linhas | confirmado | Adendo no topo da spec (renumeração fora de escopo por decisão do nó) — commit `fix(cacada-A4)` |
| A5 | Referências cruzadas | Extração de caminhos retornou `docs/audora/e` truncado — suposto caminho quebrado | Releitura do artefato real: `docs/audora/e2e/...` íntegro; o truncamento era da classe de caractere do PRÓPRIO grep da caçada (sem dígitos) | **falso-positivo** | Nenhuma no repo; lição registrada nesta linha |
| A6 | Referências cruzadas | README seção "Artefatos" não listava `docs/audora/depuracao/` (novo caminho canônico da depurar) | `grep -c depuracao README.md` → 0 | confirmado | Linha adicionada — commit `fix(cacada-A6)` |
| A7 | Configuração | `${CLAUDE_PLUGIN_ROOT}` em texto de SKILL.md pode não ser expandido na leitura da skill | `grep -rn CLAUDE_PLUGIN_ROOT skills/` → 2 ocorrências (grafo, plano) | melhoria | Trocado por referência relativa ("templates/ na raiz do plugin") — commit `fix(cacada-A7-A8)` |
| A8 | Contratos/schemas | Placeholder de id inconsistente: `plano-<id-demanda>` vs `plano-<id>`, `e2e-<id-demanda>` vs `e2e-<id>` entre skills e templates | `grep -rn "id-demanda>" skills/ templates/` → 4 ocorrências em 3 arquivos | confirmado | Unificado para `<id>` em skills e template — commits `fix(cacada-A7-A8)` e `fix(cacada-A8)` |

**Resultado:** 6 confirmados (todos corrigidos e re-verificados), 1
falso-positivo (defeito na ferramenta de checagem da caçada, não no repo), 1
melhoria (aplicada). Bordas de erro e configuração: JSONs válidos, hook
executando com saída JSON válida, wrapper polyglot testado — sem achados.

**Aprendizado (registrado):** a classe "contagens/documentação viva" foi a
mais rentável — mudança de escopo (7→8 skills) deixou rastro em 4 documentos
distintos. Reforça a regra do sync da validar: doc viva atualiza no fechamento
de CADA demanda, não "depois".
